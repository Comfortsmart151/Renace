{
  "name": "renace-2.0",
  "version": "1.0.0",
  "private": true,

  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",

  

    "build-apk-debug": "npm run build && npx cap copy android && npx cap sync android && cd android && gradlew.bat assembleDebug && cd .. && node scripts/copy-apk.js",

    "build-apk": "npm run build && npx cap copy android && npx cap sync android && cd android && gradlew.bat assembleRelease && cd .. && node scripts/copy-apk.js",

    "build-aab": "npm run build && npx cap copy android && npx cap sync android && cd android && gradlew.bat bundleRelease && cd .. && node scripts/copy-aab.js",


    "build-apk-debug-safe": "node scripts/build-apk-safe.js",


    "open-android": "npx cap open android"
  },

  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/cli": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "firebase": "^12.6.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },

  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^5.0.0"
  }
}
